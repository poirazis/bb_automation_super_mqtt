{
  "type": "automation",
  "metadata": {},
  "schema": {
    "name": "bb_super_mqtt",
    "tagline": "Publish MQTT message",
    "icon": "EmailOutline",
    "description": "An MQTT Publish message Automation for Budibase",
    "type": "action",
    "stepId": "bb_super_mqtt",
    "inputs": {
      "text": ""
    },
    "schema": {
      "inputs": {
        "properties": {
          "text": {
            "type": "string",
            "title": "Log"
          }
        },
        "required": ["text"]
      },
      "outputs": {
        "properties": {
          "success": {
            "type": "boolean",
            "description": "Whether the action was successful"
          },
          "message": {
            "type": "string",
            "description": "What was output"
          }
        },
        "required": ["success", "message"]
      }
    }
  }
}
